[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "termcap"
dynamic = ["version"]
description = "Terminal capture tool - Record terminal sessions as SVG animations"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT License" }
authors = [
    { name = "rexwzh", email = "1073853456@qq.com" }
]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: BSD",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Shells",
    "Topic :: Terminals",
]
dependencies = [
    "lxml",
    "pyte",
    "wcwidth",
    "click",
    "platformdirs",
    "toml",
    "rich",
    "pillow",
    "selenium>=4.10",
    "webdriver-manager>=4.0",
]

[project.optional-dependencies]
dev = [
    "coverage",
    "pylint",
    "twine",
    "wheel",
    "pytest",
    "pytest-cov",
    "build",
    "mkdocs",
    "mkdocs-material",
    "mkdocs-minify-plugin",
    "mkdocs-static-i18n",
    "mike",
]
docs = [
    "mkdocs",
    "mkdocs-material",
    "mkdocs-minify-plugin",
    "mkdocs-static-i18n",
    "mike",
]

[project.urls]
Homepage = "https://arch.gh.wzhecnu.cn/TermCap/"
Documentation = "https://arch.gh.wzhecnu.cn/TermCap/"
Source = "https://github.com/ChatArch/TermCap"
Issues = "https://github.com/ChatArch/TermCap/issues"

[project.scripts]
termcap = "termcap.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["termcap*"]

[tool.setuptools.package-data]
termcap = ["data/*.dtd", "data/templates/*.svg"]

[tool.setuptools.dynamic]
version = {attr = "termcap.__version__"}
